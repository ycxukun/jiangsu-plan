# 复制说明

不要把这个外层文件夹拖进项目。

正确做法：

1. 打开解压后的文件夹。
2. 看到 `index.html`、`app.js`、`style-claude-force.css`、`specialty`、`students`、`content` 等文件和文件夹。
3. 全选这些内容。
4. 复制到 GitHub Desktop 当前仓库 `jiangsu-plan` 的根目录。
5. Finder 询问是否替换时，选择“替换”。

覆盖成功后，GitHub Desktop 里不应该只看到 `.DS_Store`，而应该看到多个代码文件变化。
