# Netlify 强 UI 重试版

- 保留 Netlify 部署配置：`netlify.toml`、`_redirects`。
- 保留通用文件上传、资讯中心、学生档案、中外合作详情等现有功能。
- 新增强制暖纸质感 UI：`style-claude-force.css`。
- 增加 `.gitignore`，忽略 macOS `.DS_Store`。
- 增加复制说明，避免把外层文件夹拖入仓库导致 GitHub Desktop 只显示 `.DS_Store`。
